---
# .clang-tidy (fixed)
# Configuration for clang-tidy.

# Disable everything then enable selected groups. Use a single string (no trailing commas).
Checks: >-
  -*,clang-analyzer-*,bugprone-*,performance-*,modernize-*,readability-*,
  -readability-identifier-naming,-modernize-use-trailing-return-type

WarningsAsErrors:
  - "clang-analyzer-*"
  - "bugprone-argument-comment"
  - "bugprone-assert-side-effect"
  - "bugprone-bad-signal-to-kill-thread"
  - "bugprone-copy-constructor-init"
  - "bugprone-dangling-handle"
  - "bugprone-suspicious-string-compare"

HeaderFilterRegex: '(src|include|tests)/.*'
AnalyzeTemporaryDtors: true
...
