# pylabhubConfig.cmake.in - Config file for the pylabhub package
#
# It defines the following variables:
#  pylabhub_INCLUDE_DIRS - include directories for pylabhub
#  pylabhub_LIBRARIES    - libraries to link against to use pylabhub
#  pylabhub_EXECUTABLES  - executables provided by the package
#  pylabhub_FOUND        - true if pylabhub has been found

@PACKAGE_INIT@

# --- Dependencies ---
# Find dependencies of the pylabhub::utils library.
include(CMakeFindDependencyMacro)
find_dependency(Threads REQUIRED)
find_dependency(fmt REQUIRED)
find_dependency(nlohmann_json REQUIRED)

# --- Targets ---
# Include the file containing the definitions of our imported targets.
include("${CMAKE_CURRENT_LIST_DIR}/pylabhubTargets.cmake")

# --- Legacy Variables (optional but good practice) ---
# Note: Modern CMake encourages using targets directly, but these variables can be helpful.
get_target_property(pylabhub_INCLUDE_DIRS pylabhub::utils INTERFACE_INCLUDE_DIRECTORIES)

# For EXECUTABLES, we get the LOCATION of the installed pylabhub::app target
if(TARGET pylabhub::app)
    get_target_property(pylabhub_EXECUTABLES pylabhub::app LOCATION)
endif()
